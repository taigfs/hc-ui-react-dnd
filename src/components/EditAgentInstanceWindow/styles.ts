import styled from "styled-components";
import { ToolbarContainer } from "../Toolbar/styles";

export const StyledInput = styled.input`
  width: 100%;
  padding: 8px;
`;

export const StyledToolbarContainer = styled(ToolbarContainer)`
  padding-bottom: 8px;
  padding-top: 8px;
`;
